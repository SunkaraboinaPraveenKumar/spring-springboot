spring.application.name=SpringSecurity

server.port=9090

spring.datasource.url=jdbc:h2:mem:springSecurity
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# Let Hibernate create tables before running data.sql
spring.jpa.defer-datasource-initialization=true

# Optional: Set DDL mode
spring.jpa.hibernate.ddl-auto=create-drop
# or update, create, validate

# H2 Console Configuration
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

spring.jpa.show-sql=true

#spring.security.user.name=praveen
#spring.security.user.password=spring
